name: Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Simple test
        run: |
          echo "GitHub Actions is working!"
          echo "Repository: Aging-DDR-papers-bot"
          echo "Testing expanded keywords with reproductive aging terms"